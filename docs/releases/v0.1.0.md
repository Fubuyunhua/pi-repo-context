# pi-repo-context v0.1.0 — RELEASE CANDIDATE / UNPUBLISHED

This is local release-candidate documentation. No commit, tag, package publication, or release is claimed by this note.

## Candidate scope and compatibility

- Tool-first only: `repo_context_search`, `repo_context_status`, and `/repo-context status|rebuild|doctor` are the
  primary surface. `context_vault_repo_map` is a deprecated `0.1.x` search alias planned for removal in `0.2.0`.
- There is no automatic context injection, cross-plugin API or runtime dependency. Paused S03 Planner/Renderer/
  Projection Cache work, Provider experiments, gold tasks, research reports, and legacy bench assets are not included.
- Exact tested peers are `@earendil-works/pi-coding-agent` `0.84.1` and `typebox` `1.3.7`. The package remains version
  `0.1.0` and requires Node.js `>=22.19.0`.

## Snapshot, Graph, Resolver, and freshness

Persisted Repo Map generations retain structural `schemaVersion: 1` and provenance
`pi-repo-context` / `0.1.0`, with TypeScript and optional `java-parser@3.0.1` analyzer provenance. Immutable Snapshot
handles use `repository-snapshot/v1`, content identity, workspace revision, Git HEAD, generation, dirty files, pending
paths, diagnostics, and explicit `fresh|dirty|stale|unsupported` freshness.

Repository Graph v1 (`repository-graph/v1`) and Resolver v1 are deterministic, bounded, derived in memory from one
validated Snapshot. Domain-separated node/reference/evidence/edge identities and resolution evidence preserve source
provenance; unresolved and ambiguous relations remain explicit rather than guessed. Graph state is not persisted.

A chokidar watcher marks relevant changes pending. Search and capture reconcile watcher input, dirty workspace state,
and current Git HEAD before reporting results; failures or bounded pending work are labeled rather than silently
reported fresh.

## State, migration, and rollback

The first Repo Context startup cold-builds from repository source under:

```text
${PI_CODING_AGENT_DIR}/pi-repo-context/projects/<projectId>/repo-map
```

It does not read, move, adopt, prune, or delete legacy Context Vault Repo Map state or Observation data. Canonical owned
state components are identity-checked and isolated from the legacy root. Node has no portable `openat` API, so a
same-account or privileged process racing a canonical owned-ancestor rename between validation and a path-based
operation remains a narrow residual TOCTOU risk.

Before using this candidate beside a pre-split monolith, set `repoMapEnabled:false` and `mapInjectionMode:"off"`, stop
the old session, and restart Pi. For rollback, stop/disable Repo Context, restart, retain both state roots, and return to
the reviewed pre-split checkpoint if repository behavior must be restored. No state conversion or deletion is needed.
See [`../MIGRATION.md`](../MIGRATION.md).

## Local acceptance provenance

The accepted local Phase 7 coexistence pair used Repo Context commit
`6243694de90d1a557e3f5f3b18e7aa08dc0bd1f6` and observation-only Context Vault commit
`7062879b9a3bf3ccc491ea73824fd6abeb41a6a6`. These hashes record local test provenance only; they are not release tags
and do not claim either split release exists.

## Installation and rollback of an eventual tag

Only after a reviewed immutable `v0.1.0` tag exists should users install with:

```bash
pi install git:github.com/Fubuyunhua/pi-repo-context@v0.1.0
```

Until then, use a local checkout as documented in the README. If an eventual tagged install must be rolled back, stop
Repo Context, restart Pi, select the previously reviewed immutable version or disabled state, and do not delete either
state root.

## Release checklist

- [ ] Confirm the diff is reviewed and the candidate still contains no release date or release claim.
- [ ] On Linux Node 24, run check, full tests, package smoke, watcher smoke, real Pi RPC smoke, and coverage.
- [ ] Compare the packed artifact to the exact file allowlist; confirm tests, split-internal material, research/reports,
      Vault assets, S03, and bench files are absent.
- [ ] Run `git diff --check`, forbidden-content scans, and confirm no files are staged.
- [ ] Obtain the required reviewer approval.
- [ ] Separately authorize and perform any future commit, immutable tag, push, publication, or release; verify the tagged
      install command only after the tag exists.
